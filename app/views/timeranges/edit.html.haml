%h1.heading
  = "Editing timerange %s" % @timerange.id
  
- semantic_form_for(@timerange) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', timeranges_path