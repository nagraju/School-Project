%h1.heading
  = 'New Prize'
  
- semantic_form_for(@prize) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Create'
    
%p.actions
  = link_to 'Cancel', prizes_path