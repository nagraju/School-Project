%h1.heading
  = "Editing prize_category %s" % @prize_category.id
  
- semantic_form_for(@prize_category) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', prize_categories_path