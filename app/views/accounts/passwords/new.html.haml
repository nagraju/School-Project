= title do
  = "Reset password"
= description :textile do
  = "Enter the email address you used when creating your account. Then, hit the *\"Send instructions\"* button and we will send you an email with instructions on how to reset your password."
  
= content do
  - semantic_form_for(@account, :url => account_password_url, :html => {:method => :post}) do |f|
    - flash_form_error(f, true)
    
    - f.inputs do
      = f.input :email, :required => true, :hint => t('formtastic.hints.confirmable.email') # must be specified here explicit becuase it's still the Account model
    
    - f.buttons do
      = submit_button(f, t('formtastic.actions.updatable.create'))
      
%ul.horizontal
  %li= link_to t('actions.cancel'), :back