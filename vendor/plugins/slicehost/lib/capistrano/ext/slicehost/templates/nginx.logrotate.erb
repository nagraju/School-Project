<%= nginx_path %>/logs/*.log {
  daily
  missingok
  rotate 30
  compress
  delaycompress
  notifempty
  create 640 root adm
  sharedscripts
  postrotate
    [ ! -f <%= nginx_path %>/logs/nginx.pid ] || kill -USR1 `cat <%= nginx_path %>/logs/nginx.pid`
  endscript
}